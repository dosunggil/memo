0321 메모장

* 윈도우 커맨드와 리눅스 명령창

* / 또는 \ : 폴더의 구분
    window 에서는 \ (역슬래쉬)
    linux 에서는 /

* linux에서의 ~/ 는 user 아래의 계정 이름을 생략한것

* >_ 또는 $ : prompt 

* Linux 
    $ ls : 디렉토리 내용 (list)
    $ ls al : 폴더는 파란색/ , 맨 앞에 d
              파일은 하얀색
    $ rm -rf web* (web으로 시작되는 모든것을 삭제)

* dir : 디렉토리 내용

* 공통
    . & .. 폴더 : 현재 폴더 & 부모 폴더
    cd : 디렉토리 변경 (change directory)
        cd .. = 부모 폴더로 변경


git bash 를 이용하여 파일 지우기

------------------------------------------------------
